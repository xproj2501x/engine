import { COMPONENT_TYPES } from './components';

export const ASSEMBLAGE_TYPES = {
  CELL_ASSEMBLAGE: 'CELL_ASSEMBLAGE',
  CREATURE_ASSEMBLAGE: 'CREATURE_ASSEMBLAGE',
  RENDERABLE_ASSEMBLAGE: 'RENDERABLE_ASSEMBLAGE',
  TIME_ASSEMBLAGE: 'TIME_ASSEMBLAGE'
};

export const ASSEMBLAGES = {
  CELL_ASSEMBLAGE: {
    COMPONENTS: [
      COMPONENT_TYPES.HEALTH_COMPONENT,
      COMPONENT_TYPES.POSITION_COMPONENT
    ],
    DEFAULTS: {
      HEALTH_COMPONENT: {
        MAX_HEALTH: 0,
        CURRENT_HEALTH: 0
      }
    }

  },
  CREATURE_ASSEMBLAGE: {
    COMPONENTS: [
      COMPONENT_TYPES.HEALTH_COMPONENT,
      COMPONENT_TYPES.POSITION_COMPONENT,
      COMPONENT_TYPES.STATS_COMPONENT,
      COMPONENT_TYPES.HUNGER_COMPONENT
    ]
  },
  RENDERABLE_ASSEMBLAGE: {
    COMPONENTS: [
      COMPONENT_TYPES.POSITION_COMPONENT,
      COMPONENT_TYPES.SPRITE_COMPONENT
    ]
  },
  TIME_ASSEMBLAGE: {
    COMPONENTS: [
      COMPONENT_TYPES.TIME_COMPONENT
    ]
  }
};
